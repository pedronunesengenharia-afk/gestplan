node_modules/
dist/
.env
.env.homolog
.env.local
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
supabase/.branches
supabase/.temp
# supabase/config.toml VAI versionado: e a configuracao do Supabase local, sem
# segredo nenhum — tudo sensivel nele e referencia `env(...)`. Fora do Git, o
# `supabase link` da integracao continua nao acha com que projeto falar.
*.tsbuildinfo

# lixo do Python, do validador de paleta
__pycache__/
